# Mike's zshrc — Common configuration
# Loaded by both zshrc_personal and zshrc_work
# Requires: $DOTFILES_DIR already set by the sourcing file

# Load all modules in order from configs/shell/modules/
for module in "$DOTFILES_DIR"/configs/shell/modules/*.zsh(N); do
  source "$module"
done
unset module
